!(function(a){if(!a.jalios){a.jalios={}}if(!a.jalios.smartPhone){a.jalios.smartPhone={}}a.jalios.smartPhone.BottomBar={setBadge:function(b){if(!b){return}let selector=b.selector||"";if(!selector){return}let css=b.css||"";let text=b.text||"";let $targetItem=a(selector);if(!$targetItem.length){return}let $previousBadge=$targetItem.find(".bottom-nav-item-badge");let count=parseInt(text,10);if(count===0){$targetItem.removeClass("has-badge");if($previousBadge.length){$previousBadge.remove()}return}let badgeClasses="bottom-nav-item-badge fade in";if($previousBadge.length){$previousBadge.removeClass();$previousBadge.addClass(badgeClasses)}let $badge=$previousBadge.length?$previousBadge:a("<SPAN>").addClass(badgeClasses);$badge.addClass(css);if(count<10){$badge.addClass("one-digit")}else{if(count>=10&&count<100){$badge.addClass("two-digit")}else{if(count>=100){$badge.addClass("three-digit");text="99+"}}}$badge.text(text);$targetItem.addClass("has-badge");if(!$previousBadge.length){$targetItem.append($badge)}}}})(window.jQuery);